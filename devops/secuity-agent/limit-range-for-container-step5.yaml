apiVersion: v1
kind: LimitRange
metadata:
  name: ci-limits
  namespace: jenkins
spec:
  limits:
    - default:
        cpu: "2"
        memory: "2Gi"
      defaultRequest:
        cpu: "500m"
        memory: "512Mi"
      type: Container
